@model IEnumerable<SmartTalo.Models.ValoViewModel>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Koodi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tyyppi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tila)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Valonmaara)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Koodi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tyyppi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tila)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Valonmaara)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>*@
    </tr>
}
    
</table>
<p><a class="btn btn-danger" href="http://localhost:1865/Valo/Test">Takaisiin &raquo;</a></p>
